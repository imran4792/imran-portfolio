import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Pattan Mohammad Imran Khan - Java Full Stack Developer',
  description: 'Portfolio of Pattan Mohammad Imran Khan, a Java Full Stack Developer with expertise in Spring Boot, DevOps, and scalable web applications.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}